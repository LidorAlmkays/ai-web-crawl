# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @web-crawling/WebCrawling`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t web-crawling-webcrawling`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system web-crawling-webcrawling && \
          adduser --system -G web-crawling-webcrawling web-crawling-webcrawling

COPY apps/WebCrawling/dist web-crawling-webcrawling/
COPY apps/WebCrawling/package.json web-crawling-webcrawling/
RUN chown -R web-crawling-webcrawling:web-crawling-webcrawling .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix web-crawling-webcrawling --omit=dev -f install

CMD [ "node", "web-crawling-webcrawling" ]
